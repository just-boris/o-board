block('filter').elem('issue-check')(

    mix()(function () {
        return [{ elem: 'inline-field' }];
    }),

    content()(function () {
        return {
            block : 'checkbox',
            mods : { theme : 'islands', size : 'm', checked : true },
            name: 'issues_check',
            val : true,
            text : 'Issues'
        };
    })
);
