block('filter').elem('assign-check')(

    mix()(function () {
        return { elem: 'inline-field' };
    }),

    content()(function () {
        return {
            block : 'checkbox',
            mods : { theme : 'islands', size : 'm' },
            name: 'assign_check',
            val : 'false',
            text : 'Assigned to me'
        };
    })
);
