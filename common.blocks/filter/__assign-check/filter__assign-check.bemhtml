block('filter').elem('assign-check')(

    mix()(function () {
        return [{ elem: 'inline-field' }];
    }),

    content()(function () {
        return {
            block : 'checkbox',
            mods : { theme : 'islands', size : 'm', checked : true },
            name: 'assign_check',
            val : 'true',
            text : 'Assigned to me'
        };
    })
)
