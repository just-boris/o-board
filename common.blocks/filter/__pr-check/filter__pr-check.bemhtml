block('filter').elem('pr-check')(
    mix()(function () {
        return [{ elem: 'inline-field' }];
    }),

    content()(function () {
        return {
            block : 'checkbox',
            mods : { theme : 'islands', size : 'm', checked : true },
            name: 'pr_check',
            val : true,
            text : 'Pull requests'
        };
    })
);
