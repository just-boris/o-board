block('filter')(
    content()(function () {
        return [
            { elem: 'repo-select', content: this.ctx.repositories || [] },
            { elem: 'issue-check' },
            { elem: 'pr-check' },
            { elem: 'delimeter' },
            { elem: 'assign-check' },
            { elem: 'delimeter' },
            { elem: 'label-select', content: this.ctx.label || [] },
            { elem: 'date-input' }
        ];
    })
);
